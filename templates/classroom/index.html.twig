<table border="1">
{% for c in classrooms %}
<tr>
  <td>{{c.id}}</td>
  <td>{{c.name}}</td>
  <td>
  <a href="{{ path ("update_classroom", {"id": c.id}) }}">modifier</a>
  <a href="{{ path ("delete_classroom", {"id": c.id}) }}">supprimer</a>
  </td>
  
  
</tr>

{% endfor %}
</table>