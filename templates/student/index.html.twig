<table border="1">
{% for s in students %}
<tr>
  <td>{{s.id}}</td>
  <td>{{s.name}}</td>
  <td>{{s.email}}</td>
  <td><a href="{{path('delete_student',{'id':s.id})}}" >Delete</td>
  <td><a href="{{path('update_student',{'id':s.id})}}" >Update</td>
</tr>

{% endfor %}
</table>
